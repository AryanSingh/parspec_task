// @ts-ignoreimport Highlighter from "react-highlight-words";const Card = (props:any) => {  let data = props.data;  return <div style={{display: "flex", flexDirection: "column", border: "1px solid", padding: "10px", gap: 10}}>    <Highlighter      highlightClassName="YourHighlightClass"      searchWords={[props.searchStr]}      highlightStyle={{backgroundColor: "#ADD8E6"}}      autoEscape={true}      textToHighlight={data.id}    />    <Highlighter      highlightClassName="YourHighlightClass"      highlightStyle={{backgroundColor: "#ADD8E6"}}      searchWords={[props.searchStr]}      autoEscape={true}      textToHighlight={data.name}    />    <Highlighter      highlightClassName="YourHighlightClass"      highlightStyle={{backgroundColor: "#ADD8E6"}}      searchWords={[props.searchStr]}      autoEscape={true}      textToHighlight={data.address}    />    <Highlighter      highlightClassName="YourHighlightClass"      highlightStyle={{backgroundColor: "#ADD8E6"}}      searchWords={[props.searchStr]}      autoEscape={true}      textToHighlight={data.pincode}    />    <div>{data.items.map((item:any) => {      return(        <Highlighter          highlightClassName="YourHighlightClass"          highlightStyle={{backgroundColor: "#ADD8E6"}}          searchWords={[props.searchStr]}          autoEscape={true}          textToHighlight={item}        />      )})}</div>  </div>}export default Card;